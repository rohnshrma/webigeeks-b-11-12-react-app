import ReactDOM from "react-dom/client";

// import App.js
import App from "./App";

// creating root
const root = ReactDOM.createRoot(document.getElementById("root"));

// render inside root

root.render(<App />);

// react components are js functions returning html code
